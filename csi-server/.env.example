# CSI Server Environment Variables
# Copy this file to .env and update with your secure values

# MQTT Configuration
MQTT_USERNAME=csi_user
MQTT_PASSWORD=your_secure_mqtt_password_here
MQTT_ADMIN_PASSWORD=your_secure_admin_password_here

# InfluxDB Configuration  
INFLUXDB_ADMIN_USERNAME=admin
INFLUXDB_ADMIN_PASSWORD=your_secure_influxdb_password_here
INFLUXDB_TOKEN=generate-a-secure-token-here

# Redis Configuration
REDIS_PASSWORD=your_secure_redis_password_here

# Generate secure passwords with:
# openssl rand -base64 32

# Example secure password generation:
# MQTT_PASSWORD=$(openssl rand -base64 32)
# INFLUXDB_ADMIN_PASSWORD=$(openssl rand -base64 32)
# INFLUXDB_TOKEN=$(openssl rand -base64 48)
# REDIS_PASSWORD=$(openssl rand -base64 32)